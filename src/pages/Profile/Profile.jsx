import React, { useState } from "react";
import Button from "../../components/Button";
import Card from "../../components/Card";

const Profile = () => {

    return (
        <div>
            Estamos en el profile
        </div>
    );
};

export default Profile;
